spring.application.name=HarmoniStay-Backend
spring.profiles.active=local

# PostgreSQL (override in application-local.properties or env: DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWORD)
spring.datasource.url=jdbc:postgresql://db.vdczljgvpmylrscejlkm.supabase.co:5432/postgres?sslmode=require
spring.datasource.username=postgres
spring.datasource.password=supershubham
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

server.port=8888

razorpay.key_id=${RAZORPAY_KEY_ID:}
razorpay.key_secret=${RAZORPAY_KEY_SECRET:}

twilio.account.sid=${TWILIO_ACCOUNT_SID:}
twilio.auth.token=${TWILIO_AUTH_TOKEN:}
twilio.phone.number=${TWILIO_PHONE_NUMBER:}

# Set to false to disable MonthlyBillScheduler (monthly bill generation + payment reminders).
# app.scheduler.enabled=true

# Private society document storage. Keep this outside public/static folders.
harmonistay.documents.storage-path=${DOCUMENT_STORAGE_PATH:uploads/documents}
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=12MB
